name: CI

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  detect-changes:
    name: Detect Changed Paths
    runs-on: ubuntu-24.04
    outputs:
      core: ${{ steps.filter.outputs.core }}
      platform_build: ${{ steps.filter.outputs.platform_build }}
      linux: ${{ steps.filter.outputs.linux }}
      windows: ${{ steps.filter.outputs.windows }}
      macos: ${{ steps.filter.outputs.macos }}
      android: ${{ steps.filter.outputs.android }}
      model: ${{ steps.filter.outputs.model }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Path Filters
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core:
              - 'src/actions.rs'
              - 'src/app.rs'
              - 'src/asr.rs'
              - 'src/audio.rs'
              - 'src/beep.rs'
              - 'src/config.rs'
              - 'src/diff.rs'
              - 'src/ffi.rs'
              - 'src/history.rs'
              - 'src/lib.rs'
              - 'src/state.rs'
              - 'src/models/**'
              - 'tests/**'
              - 'licenses/**'
            platform_build:
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'build.rs'
              - 'src/main.rs'
              - 'src/paths.rs'
              - 'src/platform/mod.rs'
            linux:
              - 'src/platform/linux/**'
              - 'scripts/linux/**'
              - 'scripts/download-official-assets.sh'
              - 'scripts/acquire-base-model.sh'
              - 'scripts/stage-release.sh'
              - 'scripts/prepare-local-assets.sh'
              - 'scripts/package-base-model.sh'
              - '.github/workflows/linux.yml'
            windows:
              - 'src/platform/windows/**'
              - 'scripts/windows/**'
              - '.github/workflows/windows.yml'
            macos:
              - 'src/platform/macos/**'
              - 'scripts/macos/**'
              - '.github/workflows/macos.yml'
            android:
              - 'android/**'
              - '.github/workflows/android.yml'
            model:
              - 'model.json'
              - 'models/**'

  linux:
    name: Linux CI
    needs: detect-changes
    if: needs.detect-changes.outputs.core == 'true' || needs.detect-changes.outputs.platform_build == 'true' || needs.detect-changes.outputs.linux == 'true' || needs.detect-changes.outputs.model == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/linux.yml
    with:
      upload_release: false

  windows:
    name: Windows CI
    needs: detect-changes
    if: needs.detect-changes.outputs.platform_build == 'true' || needs.detect-changes.outputs.windows == 'true' || needs.detect-changes.outputs.model == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/windows.yml
    with:
      upload_release: false

  macos:
    name: macOS CI
    needs: detect-changes
    if: needs.detect-changes.outputs.platform_build == 'true' || needs.detect-changes.outputs.macos == 'true' || needs.detect-changes.outputs.model == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/macos.yml
    with:
      upload_release: false
      include_intel: false

  android:
    name: Android CI
    needs: detect-changes
    if: needs.detect-changes.outputs.android == 'true' || needs.detect-changes.outputs.model == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/android.yml
    with:
      upload_release: false
